export declare class PropertySetterFactory {
    private static _noopSetter(el, value);
    private _lazyPropertySettersCache;
    private _eagerPropertySettersCache;
    private _innerHTMLSetterCache;
    private _attributeSettersCache;
    private _classSettersCache;
    private _styleSettersCache;
    createSetter(protoElement: any, isNgComponent: boolean, property: string): Function;
    private _propertySetterFactory(protoElement, isNgComponent, property);
    private _isValidAttributeValue(attrName, value);
    private _attributeSetterFactory(attrName);
    private _classSetterFactory(className);
    private _styleSetterFactory(styleName, styleSuffix);
    private _resolvePropertyName(attrName);
}
